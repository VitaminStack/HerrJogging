<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="HerrJogging.BottomNavBar"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <HorizontalStackLayout Padding="0,12,0,8"
                          HorizontalOptions="Center"
                          VerticalOptions="End"
                          Spacing="10">
        <!-- Page1 -->
        <Frame WidthRequest="72" HeightRequest="72"
               BorderColor="Transparent"
               BackgroundColor="#313244"
               Padding="0"
               CornerRadius="18"
               HasShadow="False">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnPage1Clicked"/>
            </Frame.GestureRecognizers>
            <VerticalStackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                <Label Text="⚡" FontSize="24" HorizontalOptions="Center"/>
                <Label Text="Page1" FontSize="11" TextColor="#cdd6f4" HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Frame>
        <!-- LäufePage -->
        <Frame WidthRequest="72" HeightRequest="72"
               BorderColor="Transparent"
               BackgroundColor="#313244"
               Padding="0"
               CornerRadius="18"
               HasShadow="False">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnLäufeClicked"/>
            </Frame.GestureRecognizers>
            <VerticalStackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                <Label Text="🏃" FontSize="24" HorizontalOptions="Center"/>
                <Label Text="Läufe" FontSize="11" TextColor="#cdd6f4" HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Frame>
        <!-- KartePage -->
        <Frame WidthRequest="72" HeightRequest="72"
               BorderColor="#a6e3a1"
               BackgroundColor="#b4befe"
               Padding="0"
               CornerRadius="18"
               HasShadow="False">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnKarteClicked"/>
            </Frame.GestureRecognizers>
            <VerticalStackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                <Label Text="🗺" FontSize="24" HorizontalOptions="Center"/>
                <Label Text="Karte" FontSize="11" TextColor="#181825" HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Frame>
        <!-- Page4 -->
        <Frame WidthRequest="72" HeightRequest="72"
               BorderColor="Transparent"
               BackgroundColor="#313244"
               Padding="0"
               CornerRadius="18"
               HasShadow="False">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnPage4Clicked"/>
            </Frame.GestureRecognizers>
            <VerticalStackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                <Label Text="⭐" FontSize="24" HorizontalOptions="Center"/>
                <Label Text="Page4" FontSize="11" TextColor="#cdd6f4" HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Frame>
        <!-- Page5 -->
        <Frame WidthRequest="72" HeightRequest="72"
               BorderColor="Transparent"
               BackgroundColor="#313244"
               Padding="0"
               CornerRadius="18"
               HasShadow="False">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnPage5Clicked"/>
            </Frame.GestureRecognizers>
            <VerticalStackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                <Label Text="⚙️" FontSize="24" HorizontalOptions="Center"/>
                <Label Text="Page5" FontSize="11" TextColor="#cdd6f4" HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Frame>
    </HorizontalStackLayout>
</ContentView>
